{
  "provides" : "uefi",
  "sources" : [ "src/**" ],
  "targets" : {
    "windows-aarch64" : {
      // Extra flags to the linker for this target:
      "link-args" : [],
      // C3 libraries this target depends on:
      "dependencies" : [],
      // The external libraries to link for this target:
      "linked-libraries" : []
    },
    "windows-x64" : {
      // Extra flags to the linker for this target:
      "link-args" : [ "-nostdlib", "-ffreestanding" ],
      // C3 libraries this target depends on:
      "dependencies" : [],
      // The external libraries to link for this target:
      "linked-libraries" : []
    },
  }
}
